apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: staging

resources:
  - ../../base

patches:
  - path: ingress-patch.yaml

images:
  - name: ghcr.io/kalink-studio/mentalo
    newTag: 9bd7f0d

replicas:
  - name: mentalo
    count: 1
